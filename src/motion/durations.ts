/** Interactive hover transitions (links, buttons, nav items). */
export const MOTION_HOVER_MS = 350;

/** Section nav curved panel — expand. */
export const MOTION_CURVED_PANEL_OPEN_MS = 600;

/** Section nav curved panel — collapse. */
export const MOTION_CURVED_PANEL_CLOSE_MS = 400;

/** Active indicator slide (SectionNav, PageToc). */
export const MOTION_INDICATOR_MS = 280;

/** Active indicator opacity fade. */
export const MOTION_INDICATOR_OPACITY_MS = 300;

/** Doc page content enter animation. */
export const MOTION_PAGE_ENTER_MS = 1800;

/** Sticky title / scroll chrome opacity. */
export const MOTION_CHROME_FADE_MS = 240;

/** Curved panel SVG / clip-path control width (px). */
export const MOTION_CURVED_PANEL_WIDTH_PX = 100;

/** Bulge factor applied to grid column during curved panel animation. */
export const MOTION_CURVED_PANEL_BULGE_FACTOR = 0.55;
